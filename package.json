{
    "name": "guyantools-workspace",
    "private": true,
    "devDependencies": {
        "@commitlint/cli": "^19.8.1",
        "@commitlint/config-conventional": "^19.8.1",
        "commitizen": "^4.3.1",
        "cz-git": "^1.12.0",
        "husky": "^9.1.7"
    },
    "scripts": {
        "prepare": "husky",
        "commit": "git-cz",
        "commitlint": "commitlint --config commitlint.config.js",
        "commitlint:edit": "commitlint --config commitlint.config.js --edit",
        "native:build": "pnpm --dir multi_platform_core build",
        "native:build:debug": "pnpm --dir multi_platform_core build:debug",
        "desktop:start": "pnpm --dir desktop start",
        "desktop:package": "pnpm --dir desktop package",
        "desktop:make": "pnpm --dir desktop make",
        "start:desktop": "pnpm run desktop:start"
    },
    "pnpm": {
        "onlyBuiltDependencies": [
            "@parcel/watcher",
            "electron",
            "electron-winstaller",
            "esbuild",
            "lzma-native",
            "unrs-resolver",
            "vue-demi"
        ]
    },
    "config": {
        "commitizen": {
            "path": "node_modules/cz-git"
        }
    }
}
