# Project AGNETS.MD

* [X] 根据目前项目中的内容和代码风格，总结一版内容到当前项目根目录的AGENTS.md

# 通用UI及组件

* [X] tooltip在鼠标移入后，点击了被tooltip包裹的按钮后，页面变化，tooltip还停留在原处
* [X] tooltip好像有一个最大宽度？内容会有显示不全的情况
* [X] tooltip没有检测在页面边缘出现时的显示方式，而是超出页面的部分直接隐藏了
* [X] 统一应用中的弹出面板类型组件，如果没有已经实现的统一弹出面板组件，实现一个UI组件，功能需要包括需要被替换的所有弹出面板类型组件的功能需求
* [X] 规范应用中的动画效果，不要在需要使用时，重复的在每个vue文件实现一遍，然后将页面中所有需要弹出对话框，弹出面板的地方加上合适的动画过渡效果，在所有的tab页切换加上动画效果，页面切换时也要左右移入移除的过渡效果，所有需要出现又消失的组件、区域都加上过渡动画
* [X] 更换背景组件圆角小一点、收缩一下整体高度和内部每个地方的大小
* [X] 应用顶栏右侧的功能按钮，重构样式为正常应用的逻辑，占满全部高度，不要圆角，缩放窗口大小的图标换成通用的
* [X] 实现一个v-tooltip指令，而不是一直使用tooltip组件，相当于同一个功能两种使用方法，然后将桌面端所有使用了tooltip组件的地方切换成指令实现
* [X] 所有的可以更换背景的区域，添加支持更换文字颜色功能，更换也是更换主文字颜色，这个区域如果有不同配色的文字，都从一个主颜色来生成
* [X] 现在大部分区域的文字都可以选中复制，在除了输入框之外的区域，都禁用选中文字
* [X] 统一应用内通知，即报错信息、提示信息等，显示在应用页面的右上角，不要覆盖窗口功能按钮区域，离顶部的距离就是顶栏的高度，样式为左侧上下圆角，右侧直角紧贴应用右侧边缘

# 首页

+ [X] 首页组件区域的视频背景，播放一会就会停止，当鼠标点击区域后，等一会又会恢复播放。但是小组件的视频背景不会自动停止
  + [ ] 预期：自动循环播放
+ [X] 首页顶部栏添加圆角、宽度对齐下方组件区域
+ [X] 首页左侧栏中的按钮现在背景几乎是全透明，修复。现在的3d动画不好看，呼吸的效果会让按钮原本的内容放大时被边界遮挡
+ [X] 现在所有的小组件右键菜单都有打开这个动作，但是不是所有的小组件都有打开这个动作，改为只有存在打开这个动作的，右键菜单才显示打开
+ [X] 组件区域空白处右键按住，可以拖动x轴，y轴
+ [X] 完善之前的配置文件项，即可以通过切换配置文件实现切换首页的分类类别、小组件、背景设置等，应该是有预留的，现在要完善它，然后切换配置文件按钮，放在应用顶部栏，效果类似于大部分软件的顶部栏展开一个区域里面是选项，不要用选择框

# BottomBar

* [X] 现在应用底栏的拖拽状态只能在运行时保留，一刷新页面就会恢复默认的状态，实现持久化底栏排序状态

# 终端

+ [X] 新增/编辑ssh配置区域UI样式问题
  + [X] 预期：减小输入框的圆角、滚动条使用已有组件，并注意配色、勾选框使用已有组件、输入框也采用已有组件
+ [X] 点击文件管理器时，当传输页面没有建立对应的连接时，会卡顿一下才跳转到传输页面，且加载的目录也不是终端当前的目录
  + [X] 预期：点击文件管理器后立刻跳转至传输页面，并显示loading动画，且传输页面建立连接后，自动跳转至终端内对应的目录
+ [X] 终端页面顶部的栏的按钮中的颜色，没有适配明暗主题切换
  + [X] 预期：主题切换
+ [X] 端口转发面板圆角调小一些
+ [X] 长时间不操作时，ssh连接可能已经断开，输入无响应，且渲染进程控制台报错
+ [X] 当我将一个终端转移到独立窗口后，其中的内容全部不显示，虽然可以输入内容，然后可以得到输出内容，但是在最开始变为独立窗口时，无法看到之前的输出内容，关闭独立窗口后，刚才的独立窗口中的输出也不会在原本的内容中显示，已输入未执行的内容也不显示
+ [X] ssh的独立窗口功能实现要和本地终端的保持一致，目前还是主窗口和独立窗口同步显示内容。
+ [X] 添加新建本地其他类型终端的功能，每个类型的终端要有独立的配置文件
+ [X] ssh密钥管理面板圆角小一些
+ [X] 搜索功能的面板要浮在终端上方，而且居右不要占满宽度，不要圆角，整个面板调小，上一个下一个关闭按钮用图标按钮替代文字按钮
+ [X] 顶部面板中的文字按钮用图标按钮替换掉，文本显示在tooltip中
+ [X] 监听ctrl+f，这是应用内快捷键，打开搜索功能，并聚焦到输入框，然后按住enter可以进入下一个，当搜索功能已经打开，再次输入ctrl+f则关闭搜索
+ [X] 左侧栏收起状态的展开图标不对，且没有居中，然后左侧栏的收起状态时，只显示点，输入移入时通过tooltip组件显示终端会话名称
+ [X] 终端页面时，有时候会有滴的声音，在设置中添加开关这个滴的功能
+ [X] 将终端实现为每一行都可以选中的效果，就是类似于warp这个软件的效果，每一行当鼠标移入时就会有背景高亮，并且会有命令补齐提示的效果
+ [X] 当自动转发的端口被占用时，没有实现提示效果
+ [X] 当自动转发的端口被占用时，解析出占用进程，并给出是否结束占用进程来释放此端口的选项
+ [X] ssh连接失败，只有控制台报错，没有应用内提示
+ [X] ssh连接时要有连接中的状态提示，现在看不见连接情况
+ [X] 现在无法同时进行多个ssh连接，当我点击一个连接但是还没连接成功时，这时我发起另一个连接，页面上显示的状态会顶掉上一个连接，但是后台还是在连接的，需要实现为同时显示连接状态
+ [X] 多终端连接支持同时显示的布局，比如Split Horizontal、Split Vertical、Master-Stack Layout、Dwindle Layout、Grid Layout、Tabbed Layout

# 传输

+ [X] 传输页面右侧远程目录区域，在窗口大小不够时，远程目录区域右侧被截断显示，有部分内容没有被显示

  + [X] 预期：无论窗口大小都要优先却博爱两个目录区域完整显示
+ [X] 传输页面底部栏，日志和传输队列作为底部栏顶部一个tab分页存在，通过点击切换，在传输设置中删除显示/隐藏日志，固定显示
+ [X] 传输队列中传输项的样式修改，去掉卡片，扁平化沾满整个宽度，压缩高度，进度条和文字放在同一行，并设置一个固定宽度，而非占满空白区域
+ [X] 传输队列好像不能删除正在进行的传输任务？
+ [X] 传输页面的目录比较与同步面板，显示文件和目录的区域不能正常滚动
+ [X] 传输文件进行后，不主动点击远程面板的刷新按钮，不会刷新远程的文件目录，需要监听传输队列，及时刷新显示
+ [X] ftp连接长时间不活动后，连接断开没有提示，没有手动重连方法，这时操作远程目录会报错：

```
_plugin-vue_export-helper-CKwHaA5H.js:1 Error: Error invoking remote method 'ftp:list-remote-directory': Error: listRemoteDirectory failed: failed to read remote directory: session closed
```

+ [X] ftp给左右两个目录区域加上一个选中的效果，点击落在哪个区域就切换选择，然后在终端中打开的功能，现在只能跳转到本地终端，也不能跳转到对应目录
+ [X] 传输页面的左侧栏太宽了，和终端页面的左侧栏保持一致，并提取出来，作为页面通用左侧栏宽度
+ [X] 当本地目录和远程目录显示的内容不够全部高度时，这两个区域的高度会被收缩，强制让这个区域占满整个高度
+ [X] 传输页面侧栏的图标换一换，对标终端页面，然后在侧栏收起的状态下，已保存配置按钮上不要显示配置数量
+ [X] 传输页面左侧栏中已保存服务器下的树形区域，压缩每一个部分的高度，分组名称左侧不要显示Root标签，只保留右侧的数量，分组名称下方的数量文字删除，压缩高度，最好是到一行高度。具体配置项只显示前面的SFTP标签，右侧的类型标签和连接标签都删去，压缩具体配置项的高度，树形区域每一级的偏移量减少
+ [X] 压缩目录区域每个目录项的高度，调小字体大小
+ [X] 目录区域顶部面包屑的字体大小变小，宽高压缩，强制只占一行，当内容超出一行时，前面的目录显示为省略号
+ [X] 目录区域的所有文本按钮都变为图标按钮，文本显示在tooltip中
+ [X] 目录区域的列名区域和下方目录项区域中的每一列严格对齐，每列之间，一条竖线从列名到底部，不只是到最后一个目录项
+ [X] 目录区域的选中，不用整个区域都变颜色，只需要顶部标题区域变色即可
+ [X] ctrl+a快捷键选中全部、空白区域鼠标按下，框选文件、右键菜单添加上传所选文件、空白处可以打开右键菜单，包含在本地终端打开功能、选中的文件右键菜单有外部编辑和内部编辑功能，不分本地和远程都要有这个功能
+ [X] 远程目录图片预览、文本文件预览功能，右键菜单和顶栏都要有对应按钮
+ [X] ftp传输文件夹时，当这个文件夹中有很多的嵌套文件夹，传输速度非常慢，也无法在传输队列中看到嵌套目录传输情况，请在传输文件夹时，在传输队列面板中实现树形查看
+ [X] ftp传输文件夹，实现一个可选传输方式，即打包成压缩包，传输到对应目录并解压后，删除两边的临时压缩包
+ [X] 删除一个远程文件夹时，没有进度提示，点击确认删除后，下一次页面有反应就是删除完毕的时候，中间的等待时间没有任何的提示
+ [X] 本地目录的快速切换工作目录中，默认添加windows系统中的各个磁盘，用于快速切换磁盘，移除打开目录输入框，将这个功能放到面包屑的最后，用于选择目录，然后快速切换工作目录这个选择框不要占一整行，放到原本目录输入框的位置。
+ [X] 支持在首页组件区域选择自定义组件的内容，现在设计两个类型的小组件，一个是显示某个分组下所有配置文件的组件，里面是一个列表，然后可以双击列表项跳转到传输界面并打开对应的远程连接，另一个是某个配置文件的组件，跳转到传输界面并打开对应的远程连接
+ [X] 远程的目录也要支持配置快速目录，按远程连接的配置来加载
+ [X] 目录的路径输入框会有拼写提示，就是文字下方的红色波浪线，移除这个
+ [X] 目录区域的滚动条是覆盖了列名区域和列表项区域，应该只显示在列表项区域中

# Webview模块

+ [X] 在首页通过小卡片打开的webview网页时，在底部栏关闭后，并没有真的关闭这个webview，而是隐藏掉了
  + [X] 预期：点击底部栏关闭按钮时，弹出选择，是隐藏还是真的关闭，如果是隐藏的话，记得维护一个列表，后续会在一个地方显示后台已加载的网页用于恢复

# Todo

* [ ] 当切换应用主题时，此页面完全没有适配暗主题颜色

# 设置

* [X] 将当前基础设置分页中的终端部分、快捷键部分独立成一个tab栏
* [X] 基础配置中支持配置应用底栏默认显示的tab标签
* [ ] 设置页面每一个tab页都支持个性化配置
* [ ] 设置页的搜索功能实现，匹配每一设置项的标题即可，搜索时，tab页显示为搜索，聚合所有其他tab的所有设置项
