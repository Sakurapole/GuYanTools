# 数据

* [ ] 复用桌面端rust的sqlite代码及结构，跨平台核心尽量复用，现在先复用剪贴板和sqlite相关的内容

# 页面架构

# 首页

# 剪贴板

* [ ] 剪贴板要支持：

- 任意格式的文本，支持解析文本格式（普通文本、带表情文本、markdown 文本、url 等等）
- 任意格式图片，支持解析文本格式
- 指定格式的视频
- 其余格式的文件（可以配置最大同步大小，最大不超过 1G）

- [ ] 参照桌面端方案，实现同步，rust侧代码需要复用，同步方案采用mDNS，对照桌面端应用实现

# 终端

# FTP

# 设置
